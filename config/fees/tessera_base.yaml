# Tessera (Base prop AMM) fee schedule (WHI-812).
# Fees are embedded in the KyberSwap route net output (fee_embedded_in_quote).
# KyberSwap includedSources=tessera. Gas from route response when available.
venue: tessera_base
schedules:
  - instrument_type: prop_amm
    maker_bps: null
    taker_bps: null
    default_tier: embedded
    funding_model: none
    fee_embedded_in_quote: true
    # unvalidated engineering estimate (display fallback only; live quotes use quoter/route gas)
    gas_estimate_usd: "0.10"
    source_urls:
      - https://docs.kyberswap.com/kyberswap-solutions/kyberswap-aggregator/developer-guides/get-route
    updated_at: "2026-08-03T00:00:00Z"
