# Quote aggregator (WHI-807).
# Defaults are **unvalidated** pending docs/DESIGN.md §2 — engineering starting points only.

# Per-venue asyncio timeout around adapter.get_quote / get_orderbook_spread.
venue_timeout_sec: 3.0

# Short-TTL response cache to protect upstream rate limits (Jupiter keyless ~0.5 RPS).
response_cache_ttl_sec: 2.0
